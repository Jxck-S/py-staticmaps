# Requirements for developing py-staticmaps.
#
# The runtime requirements are deliberately not pulled in with --requirement:
# GitHub's dependency graph evaluates each file on its own, so a relative
# reference to another file breaks it and leaves the pip graph empty. Install
# requirements.txt alongside this one; the Makefile and CI both do.
#
# The documentation toolchain lives in requirements-docs.txt.

# Linting/Tooling
autopep8
black
codespell
flake8
isort
mypy
pur
pycodestyle
pyflakes
pylint

# Testing
coverage
packaging
pytest
pytest-cov

# Building
twine
